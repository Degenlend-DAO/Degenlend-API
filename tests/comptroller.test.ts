import { expect } from "chai";


describe("Comptroller test suite", () => {

    it('should pass a basic test', () => {
        expect(true).to.equal(true);
    }
    );

    it('should enter the markets', () => {
        // Enter the markets
        // Check if the markets were entered
    });

    it('should exit the market', () => {
        // Exit the market
        // Check if the market was exited
    });

    it('should get the accounts liquidity', () => {
        // Get the accounts liquidity
        // Check if the accounts liquidity is correct
    });

    it('should get the assets in the account', () => {
        // Get the assets in the account
        // Check if the assets in the account are correct
    });

});